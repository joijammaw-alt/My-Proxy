version: '3.8'
services:
  nginx-proxy:
    image: nginx:alpine
    container_name: blayblay-nginx-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./conf/nginx.conf:/etc/nginx/conf.d/default.conf